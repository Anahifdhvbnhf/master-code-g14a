/* eslint-disable no-unused-vars */
// eslint-disable-next-line prefer-const
let name1 = 'hola';
const age = 27;

// eslint-disable-next-line no-var
const isLive = true;
const isLive1 = true;

// eslint-disable-next-line no-console
console.log(`${name1} ${isLive1}`);
// eslint-disable-next-line no-console
console.log(`${isLive} ${isLive1}`);
